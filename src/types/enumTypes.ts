export enum ModalContentType {
  LOGOUT = '이미 로그인되어 있습니다. 로그아웃 하시겠습니까?',
}

export enum NotificationMessageType {
  LOGOUT = '로그아웃 완료',
}

export enum ButtonTextType {
  OK = '확인',
  DELETE = '삭제',
  CANCEL = '취소',
  LOGOUT = '로그아웃',
}
